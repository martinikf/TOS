@model IEnumerable<TOS.Models.Group>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
TODO: Search input here |--------------------------------|
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Creator)
            </th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model) {
      
    <tr>
        <td>
            <a asp-action="Index" asp-controller="Topic" asp-route-groupName="@item.Name">
                @Html.DisplayFor(modelItem => item.Name)
            </a>
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Creator.FirstName)
            @Html.DisplayFor(modelItem => item.Creator.LastName)
        </td>
    </tr>
  
    }
    </tbody>
    
    <!--
    <a asp-action="Edit" asp-route-id="item.GroupId">Edit</a> |
    <a asp-action="Details" asp-route-id="item.GroupId">Details</a> |
    <a asp-action="Delete" asp-route-id="item.GroupId">Delete</a>
    -->
    
</table>
